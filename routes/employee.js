//TODO: Create CRUD to employee.

const employees = [];

// Create: employees.push(employee);

// Read: employees.find(el => el.id === Number(id));

// Update: employees.find/filter(el => el.id ===/!== Number(id));
// employees.push(employee);

// Delete: employees.find/filter(el => el.id ===/!== Number(id));
// employees.push(employee);